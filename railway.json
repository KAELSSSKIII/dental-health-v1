{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "if [ -f server/package.json ]; then npm ci --prefix server; else npm ci; fi"
  },
  "deploy": {
    "startCommand": "if [ -f server/package.json ]; then npm start --prefix server; else npm start; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
