{
  "$schema": "https://schema.railpack.com",
  "build": {
    "installCmd": "if [ -d server ]; then cd server; fi; npm ci"
  },
  "deploy": {
    "startCommand": "if [ -d server ]; then cd server; fi; npm start"
  }
}
